export enum LcxEnvironments {
  production = 'PRODUCTION',
}

export enum OrderSide {
  BUY = 'BUY',
  SELL = 'SELL',
}

export enum OrderStatus {
  CANCEL = 'CANCEL',
  CLOSED = 'CLOSED',
}

export enum OrderType {
  LIMIT = 'LIMIT',
  MARKET = 'MARKET',
}
